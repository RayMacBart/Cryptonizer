// (c) Reinhard Höbart
'use strict';

class key {
   constructor(enteredKey=false) {
      this.additionKey = new AdditionKey();
      if (enteredKey) {
         this.key = this.extract(enteredKey);
      }
   }

   extract = (enteredKey) => {

   }
}

class AdditionKey {
   constructor() {
      this.
   }
}